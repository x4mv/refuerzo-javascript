const activo = true;
/* 
let mensaje = " "; 

if ( activo ){
    mensaje = ' activo';
}else{
    mensaje = ' inactivo ';
} */

// operadores ternarios
// const mensaje = ( activo ) ? 'activo ' : 'inactivo'

const mensaje = activo && 'Activo'

console.log(mensaje);